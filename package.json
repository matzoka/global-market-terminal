{
  "name": "global-market-terminal",
  "version": "4.0.0",
  "private": true,
  "description": "Read-only market terminal with provenance-aware market data: global equities, indices, FX, crypto, and metals in one screen.",
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --import ./test/setup-env.mjs --test",
    "verify:provider": "node scripts/verify-provider.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
