{
  "name": "gmt1-market-terminal",
  "version": "3.2.0",
  "private": true,
  "description": "Private, read-only market terminal with provenance-aware market data.",
  "type": "module",
  "scripts": {
    "start": "node server/server.mjs",
    "dev": "node --watch server/server.mjs",
    "test": "node --test",
    "verify:provider": "node scripts/verify-provider.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
